import { createStubJob } from "../jobs/create-stub-job";

export const expireRunnerUpOffers = createStubJob("offers.expireRunnerUp", [
  "Scaffolded job only.",
  "Future implementation will expire manual runner-up offers after the configured response window."
]);
